\section{Random Variation in Genetic Inheritance}
\label{sec:genetics}
Genetic variants are not randomly assigned, which means that simple correlations between the Ed PGI and observed outcomes cannot straightforwardly be interpreted as causal.
Genetic variation arises from inheritance: at conception, individuals inherit 23 chromosomes from each parent, obtaining half of their genetic variants from their mother and half from their father.
Because parents transmit genetic material to their children, those who inherit a higher Ed PGI typically also inherit familial environments shaped by parents who themselves possess a higher Ed PGI.
Consequently, correlations between an individual's Ed PGI and outcomes may reflect not only direct genetic effects but also indirect effects mediated through family environment, making these correlations confounded.

However, while genetic inheritance generates systematic associations between genes and environments, it also includes a random component.
Specifically, Mendelian inheritance ensures that, conditional upon parental genetics, there is random variation in the genetic variants a child inherits.
I use this random variation to estimate causal effects of the Ed PGI.

\subsection{Mendelian Independent Inheritance}
I use variation away from parental values to estimate causal effects of having a higher versus lower Ed PGI.
This approach leverages the fundamental randomness in genetic inheritance, first described by Mendel's laws \citep{mendel1865versuche}.

Each parent has 0, 1, or 2 copies of each genetic variant --- corresponding to presence on each of their chromosomes.
When a father and mother's sex cells are formed, exactly one of the two copies is sorted into each reproductive cell, and is then inherited by their resulting child.
A parent with one copy therefore passes it on half the time, whereas a parent with 0 or 2 copies transmits their genetic variant with certainty.
At conception, the child receives one draw from the mother and one from the father, so the child's copy-count is simply the sum of two independent draws.

Write $x_{i,j}^\text{\textcolor{ForestGreen}{Child}}$ for the number of copies of variant $j$ that individual $i$ has (0, 1, or 2), and 
$x_{m(i),j}^\text{\textcolor{orange}{Mother}}, x_{f(i),j}^\text{\textcolor{blue}{Father}}$ for their respective parents $m(i), f(i)$.
If the mother has two copies of variant $j$ then they bequeath one copy to their child with certainty; if the father has one, then they bequeath one copy with probability $\frac 12$; if no copies, then they bequeath none with certainty.
The child then receives their other copy from their father, by the same process, giving the following representation in expectation,
\[ \Egiven{x_{i,j}^\text{\textcolor{ForestGreen}{Child}}}{
    x_{m(i),j}^\text{\textcolor{orange}{Mother}}, x_{f(i),j}^\text{\textcolor{blue}{Father}}}
    = \frac 12 \Big( x_{m(i),j}^\text{\textcolor{orange}{Mother}}
        + x_{f(i),j}^\text{\textcolor{blue}{Father}} \Big). \]

After conception, the child has inherited genetic variants from both parents --- one of the 4 possible discrete realisations, not the expected value.
This result came about purely from random chance, conditional on at least one of parent having exactly one copy of the SNP, $x_{m(i),j}^\text{\textcolor{orange}{Mother}} = 1$ and/or $x_{f(i),j}^\text{\textcolor{blue}{Father}} = 1$.\footnote{
    Note both parents having 0 or 2 copies leaves $x_{i',j'}^\text{\textcolor{red}{Random}} = 0$, so that individual $i'$ inherited no random variation in variant $j'$.
}

\subsection{Mendelian Independent Inheritance in the Ed PGI}
\label{sec:mendel-pgi}

Senan note: include the random component figure here.

Moving from a single variant to the PGI simply means summing across the same random-segregation for all the variants.
The Ed PGI is constructed by summing the 0, 1, or 2 copy indicators across the $J \approx 8,000$ different SNPs, each multiplied by the externally estimated weights $w_{j'}$ that capture how strongly each SNP predicts education years.
\[ Z_i^\text{\textcolor{ForestGreen}{Child}}
= \sum_{j = 1}^{J} w_j x_{i,j}^\text{\textcolor{ForestGreen}{Child}}, \]

Because every variant copy enters the child's genome through an independent random draw (conditional on parental values), the child's Ed PGI is the weighted sum of thousands of such draws.
This means the Ed PGI can be decomposed into the component that was inherited as a result of a random draws from parent(s) who had 1 copy of the variant, and the certainty component from draws where parent(s) had 0 or 2 copies.
\begin{equation}
    \label{eqn:pgi-child}
    Z_i^\text{\textcolor{ForestGreen}{Child}}
    = \Egiven{Z_i^\text{\textcolor{ForestGreen}{Child}}}{
        Z_{m(i)}^\text{\textcolor{orange}{Mother}},
            Z_{f(i)}^\text{\textcolor{blue}{Father}}} + u_i,
\end{equation}
where $u_i$ is a mean zero random error term arising from Mendelian independent assortment.
The only determining factor for $u_i$ is the randomness of inheriting exact counts of SNPs $x_{i,j}^\text{\textcolor{ForestGreen}{Child}}$ from their parents, relative to what they were expected to inherit, $\frac 12 \Big( x_{m(i),j}^\text{\textcolor{orange}{Mother}} + x_{f(i),j}^\text{\textcolor{blue}{Father}} \Big)$.

Ideally, the random genetic component $Z_i^\text{\textcolor{red}{Random}}$ is constructed from observed parental Ed PGI values, $Z_{m(i)}^\text{\textcolor{orange}{Mother}}, Z_{f(i)}^\text{\textcolor{blue}{Father}}$.
However, parental PGI values are not observed for a large enough sample in the UKB.
Thus, I follow \cite{young2022mendelian} and leverage siblings' genetic data to impute parental PGI values.
This random component, therefore, is using the same within-family variation that early genetic studies did by examining within family differences.
An individual's Ed PGI is drawn from the same distribution as their sibling, because they had the same parents; in absence of observing both parents directly, this approach uses the sibling to infer this distribution.

\begin{figure}[h!]
    \centering
    \singlespacing
    \caption{Distribution of the Ed PGI and Mendelian Random Error, Relative to Imputed Parental Decile.}
    \begin{subfigure}[b]{0.495\textwidth}
        \centering
        \caption{Ed PGI.}
        \includegraphics[width=\textwidth]{sections/figures/edpgi-self-dist.png}
        \label{fig:edpgi-self-dist}
    \end{subfigure}
    \begin{subfigure}[b]{0.495\textwidth}
        \centering
        \caption{Ed PGI minus imputed parental mean.}
        \includegraphics[width=\textwidth]{sections/figures/edpgi-random-dist.png}
        \label{fig:edpgi-random-dist}
    \end{subfigure}
    \label{fig:edpgi-dist}
    \vspace{-1cm}
    \justify
    \footnotesize
    \textbf{Note}:
    This figure shows the distribution of Ed PGI, $Z_i^\text{\textcolor{ForestGreen}{Child}}$, and the Ed PGI minus imputed parental mean, $Z_i^\text{\textcolor{ForestGreen}{Child}} - \Egiven{Z_i^\text{\textcolor{ForestGreen}{Child}}}{
        Z_{m(i)}^\text{\textcolor{orange}{Mother}},
            Z_{f(i)}^\text{\textcolor{blue}{Father}}} $, conditional on the decile of the parental imputed Ed PGI.
    Parental imputed Ed PGI decile 1 shows the distributions for the bottom 10\% of parental imputed values, decile 2 shows distribution among 10--20\%, and so on.
\end{figure}

This approach is equivalent, in expectation, to using observed parents because of the simple fact that children and parents are two independent draws from the parental genetic distribution.
Thus, in expectation, they both proxy for the parental PGI values,
\[ \E{\frac 12 \left( Z_i^\text{\textcolor{ForestGreen}{Child}} 
        + Z_{s(i)}^\text{\textcolor{purple}{Sibling}} \right)}
    = \frac12 \Big(
        Z_{m(i)}^\text{\textcolor{orange}{Mother}}
            + Z_{f(i)}^\text{\textcolor{blue}{Father}} \Big). \]
However, because siblings collectively reveal approximately 75\% of the parental genetic variation, the resulting imputed values contain measurement error compared to the ideal (but unobserved) parental mean.
The sibling-imputed parental mean PGI can be viewed as a proxy for the true parental mean PGI, inflating the variance in error term $\nu_i$ in \autoref{eqn:pgi-child} denotes the measurement error arising from the incomplete observation of parental genotypes.
Notably, however, this error reduced precision of using the imputed proxy measure, compared true parental values.\footnote{
    Also note that PGI weights are calculated with measurement error, leading to attenuation bias relative to an infeasible truth Ed PGI with oracle weights.
    See \cite{van2023overcoming} for further discussion on measurement error of this kind.
}

Consequently, controlling for sibling imputed parental values maintains the same core identification properties as use of observed parental values --- notably, Mendelian independent inheritance --- though it inherently includes measurement error due to sibling-based imputation.
The precision of this method precision, therefore, depends on how genetically distinct each sibling pair is: sibling pairs who differ widely provide clearer signals, whereas genetically similar pairs introduce greater measurement noise.
For estimation, an Ordinary Least Squares (OLS) regression will implicitly weight observations by variance \citep{angrist1998estimating}, meaning sibling pairs with larger genetic differences disproportionately influence the estimated effects.
Thus, while the sibling-based imputation approach preserves unbiasedness in estimating genetic effects, it comes at the cost of increased standard errors and potentially reduced generalisability if sibling genetic divergence is not representative for the entire population.

\subsection{Causal Identification Strategy}
Let $D_i(z')$ denote the potential education level and $Y_i(z', D_i(z'))$ labour market outcome for individual $i$, under the hypothetical alternative realisation that $i$ had Ed PGI value $Z_i^\text{\textcolor{ForestGreen}{Child}} = z'$.
Observed education and wages/income data are a sum of their respective potential outcomes.
\[ D_i = \sum_{z'} \indicator{
    Z_i^\text{\textcolor{ForestGreen}{Child}} = z'} D_i(z'), \;\;\;\;
    Y_i = \sum_{z'} \indicator{
    Z_i^\text{\textcolor{ForestGreen}{Child}} = z'} Y_i(z', D_i(z')).
\]
Note that potential labour market outcomes $Y_i(z', D_i(z'))$ are a function of both hypothetical Ed PGI value, and resulting education $D_i(z')$; \autoref{sec:direct} returns to this distinction.

\noindent
\textbf{Target Parameters.}
This study aims to estimate the average causal effect of having a higher Ed PGI, which is summarised by the following average partial effects:
\[ \partialdiff{z'} \E{D_i(z')}, \;\;\;\; 
    \partialdiff{z'} \E{Y_i(z', D_i(z'))}. \]
The first average partial effect represents the average effect of a one standard deviation change in the Ed PGI on a measure of education, and the second on labour market wages or income, \textit{ceteris paribus}.

The Ed PGI, however, is neither randomly assigned nor directly manipulatable, so that we may not observe $Y_i(z', D_i(z'))$ for different values of $z'$ for the same individual, and causal effects are not identifiable without further assumptions.
Thus, I rely on controlling for parental (imputed) Ed PGI values for causal inference.

\textbf{Senan Note: Fix all of this, and make it simple conditional independence.}

\noindent
\textbf{Identifying Assumptions.}
I use the random component of the Ed PGI, $\tilde Z_i^\text{\textcolor{red}{Random}}$, as an instrument for the Ed PGI, $Z_i^\text{\textcolor{ForestGreen}{Child}}$.
This requires a number of assumptions to recover valid average causal effects.
\begin{enumerate}[label=(\arabic*)]
    \item Instrument Relevance and Independence: The random component of the Ed PGI strongly predicts the individual's Ed PGI, and is unrelated to potential outcomes.
    \item Instrument Monotonicity: 
    A higher value of the random component (weakly) increases the Ed PGI, with probability 1.
    \item Exclusion Restriction:
    The random genetic component, $\tilde Z_i^\text{\textcolor{red}{Random}}$, affects the potential outcomes only via the Ed PGI, $Z_i^\text{\textcolor{ForestGreen}{Child}}$.
\end{enumerate}

The three identifying assumptions are justified by the biology of genetic inheritance and the design of the empirical strategy.
The relevance and independence assumption (1) holds because the random genetic component arises solely from Mendelian inheritance, ensuring both predictive strength (see \autoref{tab:genetic-firststage}) and independence from unobserved confounding conditional on parental genetics and observed covariates.
The monotonicity assumption (2) is satisfied because inheriting a higher random genetic component can only additively increase the Ed PGI, never reducing it.
Indeed, the random component mechanically changes the Ed PGI for anyone who receives a higher random component, so that everyone is a complier, too.
Finally, the exclusion restriction (3) is credible given that genetic variants used in the random component influence socioeconomic outcomes only through their aggregation into the Ed PGI, by construction.

\noindent
\textbf{Estimation Strategy.}
I estimate the genetic first-stage as follows, 
\begin{equation}
    \label{eqn:genetic-firststage}
    Z_i^\text{\textcolor{ForestGreen}{Child}}
        = \gamma + \theta \tilde Z_i^\text{\textcolor{red}{Random}}
            + \vec\zeta' \vec X_i + \eta_i,
\end{equation}
where $\theta$ is the first-stage average contribution of the random component (with measurement error), and $\vec X_i$ is a vector of controls including parental imputed PGI levels, gender, age, sibling count, in city indicator, and the other PGIs (i.e., all demographic variables in \autoref{fig:demographic-correlates}).
Since $\tilde Z_i^\text{\textcolor{red}{Random}}$ serves as a formula instrument, it must be re-centred with respect to the control variables to produce valid causal effects \citep{borusyak2023nonrandom}.
I do this by calculating
$\Egiven{Z_i^\text{\textcolor{ForestGreen}{Child}}}{
    Z_{m(i)}^\text{\textcolor{orange}{Mother}},
        Z_{f(i)}^\text{\textcolor{blue}{Father}}}$
with interactions for the imputed parental PGI values and demographic variables.

I estimate the second-stage with a regression specification as follows, where $\vec X_i$ is a vector of control variables described previously.
\begin{align}
    \label{eqn:parametric-secondstage}
    Y_i &= \alpha + \beta Z_i^\text{\textcolor{ForestGreen}{Child}}
        + \vec \varphi' \vec X_i + \varepsilon_i.
\end{align}
$\beta$ represents the average effect of the Ed PGI on the education variables, and on wages and annual income (measured in $\log$ values).
I use the Ed PGI random component, $\tilde Z_i^\text{\textcolor{red}{Random}}$, to instrument for the Ed PGI in the IV specifications, using the ORIV method (with Ed PGI calculated using two independent weights) where mentioned.
The IV first-stage is shown in \autoref{eqn:genetic-firststage}, with the same set of control variables as the second-stage.

\noindent
\textbf{Limitations.}
The OLS and IV estimates place greater weight on individuals with larger deviations in the random component, $\tilde Z_i^\text{\textcolor{red}{Random}}$, which might not represent effects for the entire population --- potentially limiting external validity.
Additionally, despite independent Mendelian inheritance, the random genetic component remains modestly correlated with other PGIs.
While I control for these other PGI values, these correlations further constrain generalisability beyond the analysis sample, by implying overlap with the Ed PGI and these other PGIs.
Finally, the linear specifications employed (OLS and ORIV) yield weighted averages of individual-level causal effects.
This linear specification might not fully reflect heterogeneity in causal effects across the PGI distribution, or provide weights for the average causal effects representative for the entire UKB population --- see \cite{blandhol2022tsls} for further details.